# Uniform versus phase NVFP4 recheck

This isolated check re-runs the existing uniform dense-NVFP4 checkpoint with
the current fixed-block vLLM prompt-logprob evaluator.  It does not rebuild or
alter the baseline checkpoint.
