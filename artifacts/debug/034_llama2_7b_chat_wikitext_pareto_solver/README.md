# Llama2 WikiText predicted Pareto frontier

This directory contains a **predicted debug frontier**. Quality uses the WikiText normalized pooled proxy from debug 033; speed is raw `KernelLatencyPredictor` linear latency. Points are not measured vLLM E2E results and must be independently validated before promotion.
